<% include partials/header %>

<h1>All the blogs 2</h1>

<% blogs.forEach(function(blog){ %>
    <div>
        <p> <%= blog.title %> </p>
        <img src="<%= blog.image %>"> </p>
        <span> <%= blog.created %> </span>
    </div>
<% }); %>

<% include partials/footer %>